# Internationalization (i18n) — `apps/web` (SEA rollout)

**Status:** **Implemented** for the Vite SPA under **`apps/web`** using **i18next** + **react-i18next** + **i18next-browser-languagedetector**. Canonical authoring locale is **`en`**; active locales are **`en`**, **`ms`**, **`id`**, **`vi`**.

**Source of truth layout:** [`apps/web/src/share/i18n/`](../../apps/web/src/share/i18n/) — runtime bootstrap (`config.ts`, `policy.ts`, `format.ts`), per-locale JSON under `locales/<lng>/<namespace>.json`, glossary (`glossary/`), and audit outputs (`audit/`). Governance requires the `i18n` subdirectory under `share/` (see [`scripts/afenda.config.json`](../../scripts/afenda.config.json)).

---

## Runtime behavior

| Topic                 | Implementation                                                                                                                                                                                   |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Bootstrap**         | `initI18n()` in [`main.tsx`](../../apps/web/src/main.tsx) **before** `createRoot` renders (static bundled resources).                                                                            |
| **Default namespace** | `shell` (`defaultNS`); registered namespaces: `shell`, `auth`, `dashboard`, `invoice`, `allocation`, `settlement`.                                                                               |
| **Detection order**   | **Development:** `localStorage` → `?lng=` query → `navigator` → `htmlTag`. **Production:** `localStorage` → `navigator` → `htmlTag` (no querystring override).                                   |
| **Persistence**       | `localStorage` key **`afenda.locale`** (see [`policy.ts`](../../apps/web/src/share/i18n/policy.ts)).                                                                                             |
| **Fallback**          | `fallbackLng: 'en'`, `load: 'languageOnly'`, `supportedLngs` locked in policy.                                                                                                                   |
| **React**             | `initReactI18next`, `interpolation.escapeValue: false`, `react.useSuspense: false`.                                                                                                              |
| **Formatting**        | Shared helpers only: **`formatCurrency`**, **`formatDate`**, **`formatNumber`** in [`format.ts`](../../apps/web/src/share/i18n/format.ts) (aligned to active i18n language → BCP 47 for `Intl`). |
| **Missing keys**      | Logged in **development** via `missingKeyHandler`; production must not surface raw implementation details to users.                                                                              |

---

## Keys, namespaces, and governance

- **Canonical keys** live in English JSON only; other locales must mirror the same key paths (validated by **`pnpm run script:validate-i18n`**).
- **Release namespaces** (stricter CI): `shell`, `auth`, `dashboard` (see `RELEASE_NAMESPACES` in policy).
- **Glossary & audit:** [`glossary/canonical-terms.json`](../../apps/web/src/share/i18n/glossary/canonical-terms.json) + [`glossary/decisions.json`](../../apps/web/src/share/i18n/glossary/decisions.json); audit outputs [`audit/conflicts.json`](../../apps/web/src/share/i18n/audit/conflicts.json), [`audit/provenance.json`](../../apps/web/src/share/i18n/audit/provenance.json). Regenerate conflicts with **`pnpm run script:i18n-crossref-audit`** (sample corpora under [`scripts/data/`](../../scripts/data/)).

---

## Official documentation

**i18next** · **react-i18next** — same links as before; use for API details, pluralization, interpolation, and TypeScript resource typing.

**Optional later (per architecture plan):** `i18next-http-backend`, `i18next-cli`, `eslint-plugin-i18next`.

---

## Related documentation

- [Project structure](../PROJECT_STRUCTURE.md)
- [Testing](../TESTING.md) — validation scripts and Vitest coverage for i18n
- [React](./react.md)
- [Vite](./vite.md)
- [API reference](../API.md) — error codes → translation keys

**Context7 (AI doc refresh):** **`i18next`** → **`/websites/i18next`** or **`/llmstxt/i18next_llms-full_txt`**; **`react-i18next`** → **`/i18next/react-i18next`**.
