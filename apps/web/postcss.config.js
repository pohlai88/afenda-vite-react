export default {
  plugins: {
    // Add PostCSS plugins here
    // autoprefixer: {},
    // cssnano: process.env.NODE_ENV === 'production' ? {} : false,
    // 'postcss-preset-env': {
    //   stage: 1,
    //   autoprefixer: {
    //     grid: true,
    //   },
    // },
  },
}
