import type { AuthSessionsPayload } from "../contracts/auth-domain"

/** Placeholder for future session list projections (settings, HUD). */
export function mapAuthSessionsToViewModel(
  payload: AuthSessionsPayload
): AuthSessionsPayload {
  return payload
}
