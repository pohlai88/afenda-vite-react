# Dashboard Feature

Feature-owned dashboard surface for `/app/dashboard`.

- Owns dashboard composition and view-model data.
- Reuses `@afenda/design-system` primitives.
- Does not own shell chrome, route policy, or tenant authorization truth.
