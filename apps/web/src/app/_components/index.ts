/**
 * App-wide reusable UI (error surfaces, loading, empty states) for the Vite SPA.
 * Prefer design-system primitives for atoms; compose here for ERP-wide patterns.
 */
export * from "./app-states"
