Patch this React/TSX file directly in the repo.

Requirements:

- produce real drop-in code
- preserve valid existing logic unless deliberately improving it
- improve structure, hierarchy, rhythm, and maintainability
- update imports if needed
- avoid pseudo-code
- avoid over-abstraction
- keep the result production-safe

Before editing:

- identify the weakest areas first

Then:

- refactor the real file
- verify imports/types/local consistency
- summarize what changed and why
