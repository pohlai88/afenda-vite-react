```ts
// valid: no direct radix primitive import
import { Dialog } from "@afenda/shadcn-ui/components/ui/dialog"
import { Button } from "@afenda/shadcn-ui/components/ui/button"
```

Uses governed UI wrappers instead of `@radix-ui/react-*` imports.
