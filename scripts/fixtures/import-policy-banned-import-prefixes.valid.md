```ts
// valid: no direct radix primitive import
import { Dialog } from "@afenda/shadcn-ui-deprecated/components/ui/dialog"
import { Button } from "@afenda/shadcn-ui-deprecated/components/ui/button"
```

Uses governed UI wrappers instead of `@radix-ui/react-*` imports.
