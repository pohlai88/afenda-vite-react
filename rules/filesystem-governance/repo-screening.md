# Repo Screening

Generated by `pnpm run script:generate-filesystem-governance-report`.

## Summary

- Governed roots: 11
- Violations: 0

## Governed Roots

- `apps/web/src/marketing/pages` : max depth 3, promoted shared roots []
- `apps/web/src/app/_features` : max depth 4, promoted shared roots []
- `apps/web/src/app/_platform` : max depth 4, promoted shared roots []
- `apps/web/src/share` : max depth 3, promoted shared roots [share]
- `apps/web/src/routes` : max depth 3, promoted shared roots []
- `apps/web/src/rpc` : max depth 3, promoted shared roots []
- `apps/api/src/modules` : max depth 4, promoted shared roots []
- `apps/api/src/routes` : max depth 3, promoted shared roots []
- `apps/api/src/middleware` : max depth 3, promoted shared roots []
- `apps/api/src/contract` : max depth 3, promoted shared roots []
- `apps/api/src/lib` : max depth 3, promoted shared roots []

## Violation Counts By Rule


## First Violations

- No current violations under the scoped rollout.

## Notes

- This report is scoped to the current rollout roots, not the entire repository.
- Expand the config only after the current scope remains stable and actionable.
