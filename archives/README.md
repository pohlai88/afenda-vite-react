# Archives

## `apps-api-backup-2026-04-19/`

Full snapshot of **`apps/api`** before the scaffold (source, configs, scripts). Excludes **`node_modules`**, **`dist`**, caches.

Restore by copying back over `apps/api` (after backing up the scaffold) or cherry-picking files.

## `docs-idea-retired-2026-04-22/`

Retired idea-stage documentation moved out of the active `docs/` surface during Wave C.1. These files are historical reference only and must not be linked from the active documentation index.

## `docs-superseded-2026-04-23/`

Superseded docs removed from the active `docs/` surface during docs-policy consolidation waves. These files are historical reference only and must not be linked from the active documentation index.
