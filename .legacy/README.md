# Legacy Storage

This root holds legacy reference material and backups that are intentionally outside the active workspace topology.

Rules:

- do not place active runtime code here
- keep every top-level entry declared in `rules/storage-governance/storage-governance.config.json`
- move expired or superseded material out intentionally rather than letting this root accumulate unnamed snapshots
- keep upstream source checkouts here only as dated reference entries after the active workspace package has been normalized
