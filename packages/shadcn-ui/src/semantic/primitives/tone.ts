/**
 * SEMANTIC REGISTRY — semantic-tone
 * Semantic UI aliases the governed tone registry instead of maintaining a
 * second vocabulary locally.
 */
export {
  toneSchema as semanticToneSchema,
  toneValues as semanticToneValues,
} from "../../lib/constant/semantic/tone"
export type { Tone as SemanticTone } from "../../lib/constant/semantic/tone"
