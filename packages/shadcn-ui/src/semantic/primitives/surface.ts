/**
 * SEMANTIC REGISTRY — semantic-surface
 * Semantic UI aliases the governed surface registry instead of maintaining a
 * second vocabulary locally.
 */
export {
  surfaceSchema as semanticSurfaceSchema,
  surfaceValues as semanticSurfaceValues,
} from "../../lib/constant/semantic/surface"
export type {
  Surface as SemanticSurface,
} from "../../lib/constant/semantic/surface"
