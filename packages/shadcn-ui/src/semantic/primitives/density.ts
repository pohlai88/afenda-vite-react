/**
 * SEMANTIC REGISTRY — semantic-density
 * Semantic UI aliases the governed density registry instead of maintaining a
 * second vocabulary locally.
 */
export {
  densitySchema as semanticDensitySchema,
  densityValues as semanticDensityValues,
} from "../../lib/constant/foundation/density"
export type {
  Density as SemanticDensity,
} from "../../lib/constant/foundation/density"
