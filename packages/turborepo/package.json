{
  "name": "@afenda/turborepo",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Turborepo task defaults. Root turbo.json cannot use extends (Turborepo ignores it); add packages/<name>/turbo.json with \"extends\": [\"//\", \"@afenda/turborepo\"] for tsc→dist libraries. Apps (e.g. @afenda/web) stay root-only unless they need overrides."
}
