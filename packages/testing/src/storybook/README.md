# Storybook + component / visual testing

Reserved for **shared Storybook test glue** when adopted:

- **Portable stories** + Vitest ([Storybook docs](https://storybook.js.org/docs/api/portable-stories/portable-stories-vitest))
- Shared **decorators** or **parameters** used by both Storybook and unit tests
- Pointers to **visual regression** or **a11y** tooling policy (see root `docs/TESTING.md`)

Story files and app-specific config remain in the owning app or design package.
