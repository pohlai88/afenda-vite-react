import { definePrimitiveManifest } from "./manifest-contract"

export default definePrimitiveManifest({
  owner: "design-system",
  lifecycle: "stable",
  purpose: "Canonical governance metadata for the context-menu primitive.",
})
