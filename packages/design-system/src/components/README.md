# Components

This directory now contains **`shadcn/`** only: the canonical reference UI for `@afenda/design-system`.

All legacy component folders under `src/components/` have been removed. Use **`@afenda/shadcn-ui`** for application runtime UI.

See `shadcn/README.md` for details.
