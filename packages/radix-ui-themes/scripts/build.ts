/**
 * Build script placeholder.
 *
 * This package currently uses source-level exports (consumers' bundlers
 * compile the TypeScript directly), so no build step is needed yet.
 *
 * If a pre-built dist is required in the future (e.g., for CJS/ESM dual
 * output via esbuild, mirroring the upstream Radix Themes build), this
 * script will be the entry point. See the upstream build process described
 * in AGENTS.md for reference.
 */

export {}

