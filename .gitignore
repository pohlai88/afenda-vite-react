# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist/
**/dist/
coverage
# Regenerated caches (legacy path — safe to delete locally)
.eslintcache
apps/*/.eslintcache
**/.artifacts/
_tmp_*/
.turbo
.tmp/
dist-ssr
*.local
# Env files (keep `.env.example` committed)
.env
.env.*
!.env.example

# Vite CPU profiling dumps (local only)
*.cpuprofile

# Optional local clones of upstream repos (reference only; nested .git must not be committed)
.tmp-hono-skill/
packages/vitest-config/vitest-github/
packages/vitest-config/_vitest-github/
packages/eslint-config/_eslint-github/
packages/pino-logger/pino-github/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
